[build]
builder = "DOCKERFILE"

[deploy]
preDeployCommand = "/usr/local/bin/docker-entrypoint.sh --check"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
sleepApplication = false
